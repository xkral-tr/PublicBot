{
    "name": "PublicBot",
    "keywords": ["bot", "discord bot"],
    "repository": "https://github.com/xkral-tr/PublicBot",
    "env": {
        "BOT_TOKEN": {
            "description": "Discord bot token",
            "value": "Write bot token here"
        },
        "DATABASE_URL": {
            "description": "A mongodb database connection url",
            "value": "Write database connection url here"
        }
    },
    "environments": {
        "test": {
            "env": {
                "BOT_TOKEN": "[BOT TOKEN]",
                "DATABASE_URL": "[DATABASE URL]"
            }
        }
    }
}
